{
  "name": "desktop-companion-kit",
  "version": "0.1.0",
  "description": "A configurable transparent macOS desktop companion with local page awareness and scheduled speech.",
  "license": "MIT",
  "author": "Vesper",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vesperhalcyon/desktop-companion-kit.git"
  },
  "bugs": {
    "url": "https://github.com/vesperhalcyon/desktop-companion-kit/issues"
  },
  "homepage": "https://github.com/vesperhalcyon/desktop-companion-kit#readme",
  "keywords": [
    "desktop-companion",
    "electron",
    "macos",
    "desktop-pet"
  ],
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test",
    "check": "node --check main.js && node --check preload.js && node --check lib/character-profile.js && node --check lib/commentary.js && node --check lib/inbox.js && node --check lib/model-bridge.js && node --check lib/page-observer.js && node --check lib/settings-store.js && node --check renderer/app.js && node --check scripts/probe-page.js && node --check scripts/say.js",
    "smoke:app": "DESKTOP_COMPANION_SMOKE=1 electron .",
    "probe:page": "node scripts/probe-page.js",
    "say": "node scripts/say.js",
    "package": "electron-packager . \"${DESKTOP_COMPANION_NAME:-Desktop Companion}\" --platform=darwin --arch=arm64 --out=.artifacts --overwrite --asar"
  },
  "devDependencies": {
    "electron": "latest",
    "@electron/packager": "latest"
  }
}
